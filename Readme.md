### To Start the Server

1 -
Add Server env variables in .env
MONGO_URI=
JWT_SECRET=

Note:- mongo-url should be base url, (without collection name), - server run 4000 port in default

2 - Install packages,
command to install : npm install

3 - Start the App, after place the env variable.
command to run:- npm start
